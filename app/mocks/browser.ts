/**
 * MSW Browser Setup
 *
 * This file configures MSW for browser-based mocking.
 * Uncomment and configure when ready to use MSW.
 *
 * Setup steps:
 * 1. npm install msw --save-dev
 * 2. npx msw init public/
 * 3. Uncomment the code below
 * 4. Import and start in your app entry point (for dev only)
 */

// import { setupWorker } from 'msw/browser';
// import { handlers } from './handlers';
//
// export const worker = setupWorker(...handlers);

// Placeholder export for TypeScript
export {};
