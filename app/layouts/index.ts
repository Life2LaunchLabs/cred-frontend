/**
 * Layout Components
 *
 * This directory contains layout components that define page structure.
 * When importing from MUI templates, place layout-related components here.
 *
 * Template layouts to integrate:
 * - DashboardLayout - Main dashboard shell with sidebar/header
 * - AuthLayout      - Layout for sign-in/sign-up pages (minimal, centered)
 *
 * Usage in routes:
 * Routes can specify a layout by wrapping their component or using
 * React Router's layout routes feature.
 */

// Export layouts here as they are added
// Example: export { DashboardLayout } from './DashboardLayout';
// Example: export { AuthLayout } from './AuthLayout';
